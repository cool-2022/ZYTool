## 后端示例环境变量（不要将真实凭据提交到仓库）
# PostgreSQL 示例
# DB_TYPE=postgresql
# DB_HOST=127.0.0.1
# DB_PORT=5432
# DB_USER=your_pg_user
# DB_PASSWORD=your_pg_password
# DB_NAME=zytool

# 如果使用 DB2，替换为 db2 并可设置 DB2_DBQ（或直接设置 SQLALCHEMY_DATABASE_URI）
DB_TYPE=db2
DB_HOST=10.25.145.19
DB_PORT=50000
DB_USER=bm2mms
DB_PASSWORD=bm2mms
DB_NAME=DBTESTMS

# 可直接设置完整连接字符串（优先级高于拆分字段）
#SQLALCHEMY_DATABASE_URI=ibm_db_sa://user:pass@host:port/MYDB
