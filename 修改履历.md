# 修改履历

| 修改时间 | 业务需求 | 涉及模块 | 修改内容 | 备注 |
|---|---|---|---|---|
| 2026-08-06 | 前端死代码清理 | ZYTool 前端 | 删除 SqlSplice.vue、assets/vue.svg；清理 api.ts 9 个未调用方法及关联接口；清理 frontendTools/auth/appConfig/ChatView/GCPMRuleView/SqlRation 中未使用导出与变量 | MapLocationView.vue 有未提交修改，按用户要求保留 |
| 2026-08-07 | AI 助手接入 Kimi 并重构 | Rust 后端、ZYTool 前端 | 后端 routes/agents.rs、services/agents.rs 拆分为 agents/ 文件夹（chat/sessions/store/llm），接入 Kimi 流式对话（读取 .env KIMI_* 配置）；config.rs 新增 kimi_api_key/kimi_base_url/kimi_model；前端 ChatView 新增停止生成、会话重命名、loading、打字光标 | cargo check、yarn build、Kimi 接口连通性均已验证 |
| 2026-08-07 | 建立项目知识库 | 项目根目录 | 按模板重构 AGENTS.md，建立 .trar/ 知识库（common-knowledge + rust/python/frontend 三端） | 技术细节已迁移至 .trar/ 对应文件 |
| 2026-08-07 | 修改履历约定调整 | AGENTS.md | 「记录履历」触发词改为每次完成代码修改后自动记录 | 本文件同期创建 |
| 2026-08-07 | 数据库表结构入库 | .trar/rust-backend-knowledge | 根据 `ToolsAI/zytool.sql` 整理 10 张表（认证域 4 + AI 域 6）写入 `resources/01-database-schema.md`，并在 rust SKILL.md 建立索引 | 标注了代码未接入的预留扩展点（ai_models、feedbacks 等） |
| 2026-08-07 | 修复 AI 聊天 500 错误 | Rust 后端 services/agents/llm.rs | k3-256k 模型仅允许 temperature=1，移除请求中的 temperature 参数；清理 llm.rs 中被注入的调试代码，改用 tracing::error 记录 Kimi 调用错误 | 流式/非流式均已实测通过，消息正常落库 |
| 2026-08-07 | token 过期导致会话加载失败 | Rust 后端 .env | 新增 ACCESS_TOKEN_EXPIRE_MINUTES=1440（开发环境 24 小时），避免默认 30 分钟频繁掉线 | 401 非后端故障，重新登录即可 |
| 2026-08-07 | 修复 a-textarea onKeydown 类型警告 | ZYTool 前端 ChatView.vue | 两个 @keydown.enter 修饰符处理器被编译为数组，合并为单个 handleInputKeydown 函数（Enter 发送、Shift+Enter 换行） | yarn build 通过 |
| 2026-08-07 | Python 兜底后端接入 Kimi | tools_project_py | deepseek_client 优先读取 KIMI_API_KEY/KIMI_BASE_URL/KIMI_MODEL（未配置回退 DeepSeek）；generate.py 硬编码模型名改为 deepseek_client.model；.env.development 增加 KIMI_* | 兜底链路 /agents/chat 实测通过 |
| 2026-08-07 | AI 聊天响应慢排查与优化 | Rust/Python 后端 .env | 实测延迟：k3-256k TTFB 6~12s（深度推理），后端自身开销 <1s；切换为 kimi-for-coding-highspeed（TTFB 0.8s），端到端降至 1.7~4.3s | 两端 KIMI_MODEL 已同步 |
| 2026-08-07 | 增加"正在思考"等待动画 | ZYTool 前端 ChatView.vue/.css | 首 token 返回前（流式消息内容为空时）显示三点弹跳 + "正在思考"动画，覆盖 TTFB 等待期 | yarn build 通过 |
| 2026-08-07 | 等待动画换成汤姆跑步动图 | ZYTool 前端 ChatView.vue/.css、src/assets/tom-running.gif | 思考动画改为 <img> 引用 `@/assets/tom-running.gif`（56x40），生成简易奔跑小人动图占位（原 1px 透明占位不可见） | 用户提供汤姆 GIF 覆盖该文件即可 |
| 2026-08-07 | 修复偶发 400 空消息错误 | ZYTool 前端 ChatView.ts | 根因：sendMessage 在 await 后才读 userInput，快速连按 Enter 时并发调用读到空值发出空消息；修复为提前捕获并清空输入、生成期间禁止重复发送 | yarn build 通过 |
| 2026-08-07 | 会话删除写入备份履历表 | Rust 后端 store.rs、PostgreSQL | 新建 ai_session_archives 表；delete_session 改为事务：先写入会话信息+消息快照（messages jsonb）再软删除；DDL 已追加到 ToolsAI/zytool.sql | 实测删除后履历表落库正常 |
| 2026-08-07 | 会话删除改为物理删除 | Rust 后端 store.rs | delete_session 事务第二步由 UPDATE status=3 改为 DELETE FROM ai_sessions（消息/设置/反馈随 CASCADE 清除），数据仅以 ai_session_archives 快照保留 | 实测 ai_sessions 无残留、履历正常 |
| 2026-08-07 | 清理历史软删除残留数据 | PostgreSQL | 确认 16 行 status=3 会话均已有 ai_session_archives 备份后，执行 DELETE FROM ai_sessions WHERE status=3 清理（消息随 CASCADE 清除） | ai_sessions 当前 0 行 |
| 2026-08-07 | 支持局域网访问 | ZYTool .env、Rust config.rs | 前端 API 地址改为 http://192.168.77.154:8000/8001；CORS 默认放行 http://192.168.77.154:5050/5173 及 192.168.188.2:5050/5173；vite 以 --host 重启 | 188.2 实为 VPN 隧道地址不可用于共享，已告知 |
| 2026-08-07 | Rust 后端看门狗脚本化 | tools_project_rust/start.sh | 新建 start.sh（前台/-d 后台/stop 三模式），watchdog 循环 + PID 文件管理，二进制缺失时自动 cargo build；日志 logs/server.log | 已实测 kill 后 1s 自动拉起 |
